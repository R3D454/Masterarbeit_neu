

#include <stdio.h>
#include <iostream>
#include <QTimer>
#include <marble/GeoDataTimeStamp.h>
#include <marble/GeoDataFeature.h>
#include <marble/GeoDataPlacemark.h>


int main(int argc, char** argv){


  
  // char c = 0;
  //
  // const QDateTime peter = QDateTime::currentDateTime();
  // QDateTime hans = QDateTime::currentDateTime();
  // hans = hans.addDays(50);
  // /*
  // QString sp = peter.toString("dd.MM.yyyy hh:mm:ss a");
  // std::cout<< "Zeitstempel peter:"<< sp.toUtf8().constData() << std::endl;
  // QString sh = hans.toString("dd.MM.yyyy hh:mm:ss a");
  // std::cout<< "Zeitstempel hans:"<< sh.toUtf8().constData() << std::endl;*/
  //
  // if (peter < hans){
  //   std::cout<< "peter ist kleiner als hans!" << std::endl;
  // }
  //
  // Marble::GeoDataTimeStamp gerd;
  // gerd.setWhen(peter);
  // Marble::GeoDataPlacemark *placemark = new Marble::GeoDataPlacemark();
  // placemark->setTimeStamp(gerd);
  // const Marble::GeoDataTimeStamp martha = placemark->timeStamp();
  //
  // QDateTime doris = martha.when();
  // QString s = doris.toString("dd.MM.yyyy hh:mm:ss a");
  // std::cout<< "Zeitstempel doris:"<< s.toUtf8().constData() << std::endl;
  //
  //
  //
  // std::cin >> c;
  // std::cout << "Ende:" << c << std::endl;
  return 0;
}
